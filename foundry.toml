[profile.default]
src = "script"
script = "script"
out = "out"
cache_path = "cache"
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
# No external library dependencies — scripts are self-contained
# (vm cheatcodes and contract structs are inlined in the script).
libs = []
